# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.eggs/
.venv/
.venv*/
venv/
*.egg

# Data & model weights (too large for git — ScanNet frames, not source code)
/data/
# external VGGT-Omega dependency — clone separately (see docs/architecture.md)
vggt/

# Generated outputs (reproducible, not source)
demo_outputs/

# Video build artifacts (demo goes to YouTube per submission rules, not git)
*.mp4
clips/
/_turntable_frames/
clips/_turntable_frames/
planner_lam0.png
planner_lam4.png
# the video kit itself (scripts + small caption assets) IS tracked; only the
# heavyweight rendered clips and final mp4 above are ignored.
demo_video/assets/
demo_video/audio/
demo_video/blender/
demo_video/build/
demo_video/export/
demo_video/media/
demo_video/renders/
live_demo/

# Presentation binaries (decks are generated by make_*deck.py; PDFs submitted
# separately). The tracked AX submission PDF predates this rule.
*.pptx
/OccluSynth_Final_Round*.pdf

# Scratch / debug outputs
test.glb
test.obj
test.ply
test.png
repro_step*.txt
repro_full.log
shots/
*.log

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Editors
.vscode/
.idea/
*.swp
*.swo

# Jupyter
.ipynb_checkpoints/
*.ipynb

# Secrets / env
.env
*.key
/checkpoints/
/wandb/
